package App;
import Objects.Banco;
import Objects.Conta;
import Objects.Poupanca;
import javax.swing.JOptionPane;


public class App {
    public static void main(String[] args) throws Exception {
        // Conta c = new Conta();
        // Conta c2 = new Conta(333, 0.0);
        // c.setNumero(Integer.parseInt(JOptionPane.showInputDialog("Conta")));
        // c.setSaldo(Double.parseDouble(JOptionPane.showInputDialog("Saldo em Conta:")));
        // JOptionPane.showMessageDialog(null, c.toString(), null, 0);
        // c.transferir(c2, Double.parseDouble(JOptionPane.showInputDialog("Valor para Transferir:")));
        // JOptionPane.showMessageDialog(null, c.toString(), null, 0); 
        // JOptionPane.showMessageDialog(null, c2.toString(), null, 0);
        // // c.creditar(Double.parseDouble(JOptionPane.showInputDialog("Creditar:")));
        // JOptionPane.showMessageDialog(null, c.toString(), null, 0);
        // c.debitar(Double.parseDouble(JOptionPane.showInputDialog("Debitar:")));
        // JOptionPane.showMessageDialog(null, c.toString(), null, 0);
    
        // Conta teste = new Poupanca(10);
        // teste.setNumero(Integer.parseInt(JOptionPane.showInputDialog("Conta")));
        // teste.setSaldo(Double.parseDouble(JOptionPane.showInputDialog("Saldo em Conta:")));
        // JOptionPane.showMessageDialog(null, teste.toString(), null, 0);
        // ((Poupanca) teste).renderJuros(Double.parseDouble(JOptionPane.showInputDialog("Taxa:")));
        // JOptionPane.showMessageDialog(null, teste.toString(), null, 0);

        // JOptionPane.showMessageDialog(null, teste.toString(), null, 0);
        // Banco bb = new Banco();

        // bb.cadastrar(teste);

        // double valor = Double.parseDouble(JOptionPane.showInputDialog("Creditar:"));
        // bb.creditar(teste.getNumero(), valor);
        // JOptionPane.showMessageDialog(null, teste.toString(), null, 0);

        // valor = Double.parseDouble(JOptionPane.showInputDialog("Debitar:"));
        // bb.debitar(teste.getNumero(), valor);
        // JOptionPane.showMessageDialog(null, teste.toString(), null, 0);

    }
}
